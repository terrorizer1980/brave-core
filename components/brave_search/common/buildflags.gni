import("//brave/build/config.gni")

declare_args() {
  enable_web_discovery_feature =
      brave_channel == "nightly" || brave_channel == "development" ||
      brave_channel == "dev" || brave_channel == "beta"
}
